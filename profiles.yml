config:
  send_anonymous_usage_stats: False

default:
  outputs:

    dev: # User-Password config
      type: snowflake
      account: bcg.eu-central-1
      user: SRV_DBT_CICD_SANDBOX
      password: x%zdQBNZ?2AE^a3dKjBZ
      role: SANDBOX_DBT_CICD_POC_ROLE
      database: SANDBOX
      warehouse: DEMO_WH
      schema: DBT_CICD
      threads: 2
      client_session_keep_alive: False

  target: dev
